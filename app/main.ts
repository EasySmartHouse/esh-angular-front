import { platformBrowserDynamic } from
    '@angular/platform-browser-dynamic'
import { ESHAppModule } from './esh-app.module'
//import { enableProdMode } from '@angular/core'

//enableProdMode()
platformBrowserDynamic().bootstrapModule(ESHAppModule)